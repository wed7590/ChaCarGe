<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis">
	<!-- login_ok -->
	<select id="login_ok" parameterType="net.chacarge.model1.LoginTO" resultType="net.chacarge.model1.UserTO">
		select *
		from user
		where user_id = #{login_id}
	</select>

	<!-- join_ok -->
	<insert id="join_ok" parameterType="net.chacarge.model1.UserTO">
		insert into user ( user_seq, user_id, user_password, user_email, user_name)
		values( 0, #{user_id}, #{user_password}, #{user_email}, #{user_name} )
	</insert>
</mapper>